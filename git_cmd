git checkout -b feature/add_smthg
git add .
git commit -m "test"
git diff HEAD~1 HEAD --name-only
git push -u origin feature/add_smthg

git checkout -b feature/use_java8
git add .
git commit -m "use stream"
git checkout master
git merge feature/use_java8
**fast forward**

git stash
git stash pop
git checkout temp/temp-usage
git add .
git commit -m "stash "

git cherry-pick
git commit --amend -m --no-edit

git commit squash

git rebase -i
